ul.nav-tabs(ngbNav, #nav='ngbNav')
    li(ngbNavItem)
        a(ngbNavLink, translate) General
        ng-template(ngbNavContent)
            .mb-3
                label(translate) Host
                input.form-control(
                    type='text',
                    [(ngModel)]='profile.options.host',
                )

            .mb-3
                label(translate) Port
                input.form-control(
                    type='number',
                    placeholder='22',
                    [(ngModel)]='profile.options.port',
                )

            .mb-3
                label(translate) Terminal type
                input.form-control(
                    type='text',
                    placeholder='xterm',
                    [(ngModel)]='profile.options.terminalType',
                )
                .form-text.text-muted(translate) Reported to the server as TERM. Must exist in the server's terminfo - AIX often has xterm but not xterm-256color.

            stream-processing-settings([options]='profile.options')

    li(ngbNavItem)
        a(ngbNavLink, translate) Login scripts
        ng-template(ngbNavContent)
            login-scripts-settings([options]='profile.options')

    li(ngbNavItem)
        a(ngbNavLink, translate) Input
        ng-template(ngbNavContent)
            input-processing-settings([options]='profile.options.input')

div([ngbNavOutlet]='nav')
